def message_logReader(message_log, DebugMode):

    if DebugMode:
        print(message_log)

    print(message_log)


def call_logsReader(call_logs, DebugMode):
    if DebugMode:
        print(call_logs)
